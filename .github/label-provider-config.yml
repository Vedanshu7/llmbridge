provider:openai:
  - '(openai|gpt-4|gpt-3|gpt4|gpt3|o1|o3)'

provider:anthropic:
  - '(anthropic|claude)'

provider:bedrock:
  - '(bedrock|aws)'

provider:gemini:
  - '(gemini|google|vertex)'

provider:cohere:
  - '(cohere|command)'

provider:mistral:
  - '(mistral)'

provider:ollama:
  - '(ollama|local model)'

provider:groq:
  - '(groq)'

provider:deepseek:
  - '(deepseek)'

component:proxy:
  - '(proxy|router|middleware)'

component:caching:
  - '(cache|caching|disk cache)'

component:streaming:
  - '(stream|streaming|sse)'

component:cost:
  - '(cost|pricing|token count|usage)'
